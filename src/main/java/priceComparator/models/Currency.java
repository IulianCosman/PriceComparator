package priceComparator.models;

/**
 * Enum representing the various currencies in which a product price can be specified.
 */
public enum Currency {
    RON,
    EUR,
    USD,
    // Fallback case
    UNKNOWN
}
